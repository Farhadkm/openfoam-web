[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    structural: deterministic intent/XML checks (no DeepEval judge)
    deepeval: DeepEval GEval judge (needs OPENAI_API_KEY or deepeval model config)
    slow: calls Vertex Gemini (requires GOOGLE_APPLICATION_CREDENTIALS)
